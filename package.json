{
  "name": "@sleep-tracker/root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:mobile": "npm run start --workspace=apps/mobile",
    "build:web": "npm run build --workspace=apps/web",
    "preview:web": "npm run preview --workspace=apps/web"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
